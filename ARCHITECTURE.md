# E时代服务导航架构

## 原版基线

本次重构以远端默认分支 `origin/master` 的 `ecbf485` 为内容与部署基线，不以本地 `backup/mui-workspace-20260716` 改稿为基础。原版从 2025 年的单页卡片导航逐步演进为三段信息架构：

1. **产品服务**：6 个 E时代核心基础设施；
2. **通行证生态链**：5 个已接入统一登录的应用；
3. **团队与官网**：6 个团队、实验室与开发入口。

原版的 17 个名称、描述、真实 URL、线性 SVG 图标、更新日志、页脚链接、备案号、明暗主题与 SEO 元数据均是产品事实，必须保留。在此基础上新增“成员项目”分类及“渡鸦笔记”入口，形成当前 18 个服务、4 个分类。原有第三方统计脚本不属于导航核心能力，按本次“无第三方追踪”的安全要求移除。公开域名为 `https://nav.emoera.com/`，构建、部署和回滚流程见 `docs/deploy.md`。

## 产品呈现

核心任务始终是快速找到并打开 18 个社团服务；3D 只把本地线性图标转换为可探索的真实几何：

- 四个分类保持固定方位，依次为成员项目、产品服务、通行证生态链、团队与官网；
- 18 个服务是区域内可聚焦的 3D 图标 Mesh；名称和说明继续由可访问 DOM 提供；
- `总览 → 分类 → 服务` 是唯一层级，相机位移表达层级，路径栏同步表达当前位置；
- 搜索结果直接聚焦服务节点；选择节点先展示详情，详情中的访问操作一次打开静态白名单外链；
- DOM 目录与 Canvas 使用同一份数据和导航状态，键盘、读屏、搜索和降级模式不会成为第二套产品。

## 运行时分层

### 1. 数据与状态层

- `src/data/services.js`：18 个服务、4 个分类、固定空间坐标和本地图标键；
- `src/icons/originalIconRegistry.js`：可审计的 `serviceId → originalIcon → geometry` 语义色映射，以及唯一可信 SVG 元素源；
- `src/lib/navigation.js`：URL 状态解析、白名单化、历史记录与安全外链校验；
- `src/lib/preferences.js`：主题、最近访问、引导状态的容错本地存储；
- URL 只接受数据表中存在的 category/service slug，恶意或陈旧参数会被忽略。

### 2. DOM 交互层

- 顶部控制台固定为左侧品牌/上下文路径、中间全局搜索、右侧模式/主题/帮助三段；总览不显示冗余路径，平板和移动端将上下文路径放入次级栏；
- 服务详情、外链确认与最近访问均为可聚焦、可读屏的标准 DOM；
- 2D 服务列表始终是语义内容源，WebGL 不可用时直接显示同一套卡片；
- `index.html` 提供静态元数据、结构化数据和 `noscript` 服务链接，Canvas 不承载 SEO。

### 3. 三维表现层

- `src/scene/SpatialScene.jsx` 通过 `React.lazy` 独立分包，`ExtrudedServiceIcon` 负责实际 Mesh；
- `SVGLoader` 从 `ICON_DEFINITIONS` 的本地 path 解析原轮廓；stroke 走 `TubeGeometry`，fill path 走 `createShapes + ExtrudeGeometry`；
- 合并后的 `BufferGeometry` 带 position、normal、index 与非零 Z 厚度，并按 icon/quality 缓存；
- 2D 列表、搜索、详情与 3D 几何共享同一 `ICON_DEFINITIONS`；3D 场景不使用图像、纹理或 DOM 显示 icon；
- focus 时旋转图标展示侧壁，hover 只轻抬并增强材质响应；reduced-motion 使用静态正视；
- DPR 有上限，性能下降时主动降低像素比并可回退 2D，页面隐藏时停止渲染。

### 4. 品牌资产层

- 官方来源 `https://we.emoera.com/img/elogo.jpg` 只在构建阶段下载，运行时全部读取 `public/brand/` 本地资产；
- 96/192/512 WebP 与 PNG 按显示密度选择，64 PNG 用于 favicon，1200 × 630 PNG 用于分享图；
- `public/brand/README.md` 记录来源、日期、原始尺寸、派生方式和“不得用于服务模块”的边界。

## 导航与动画约束

- 相机目标始终由当前 URL 空间状态推导，因此前进、后退和深链可恢复同一视图；
- 相机控制允许在程序动画中被拖拽、缩放和重新定向，不锁死输入；
- 按下即反馈，过渡从当前呈现值继续；非手势运动使用高阻尼、无弹跳行为；
- `prefers-reduced-motion` 下取消大范围飞行和环境漂移，改为极短切换或静态视图；
- Escape 后退，H 回到总览，`/` 搜索，`?` 打开快捷键帮助。

## 能力检测与降级

启动时检查 WebGL、逻辑核心数、设备内存、节流网络和数据节省偏好。WebGL 不可用或设备明显受限时默认进入 2D。Canvas 运行时异常由错误边界捕获并切换 2D；用户始终可以手动切换模式。

## 性能预算

- 首屏 DOM 主包目标：压缩后小于 180 KiB（不含懒加载三维包）；
- 三维依赖独立分包并仅在适合的设备加载；
- 17 种图标按 high/low 两档各缓存一次，两个 globe 服务共享轮廓；
- 18 个服务图标实例累计 high 为 11,232、low 为 5,024 三角形，整帧峰值目标低于 110 Draw Calls；
- 图标不使用纹理显存；文字 HUD 是独立 DOM，不构成 icon 本体；
- 场景无业务纹理、无外链模型、无粒子和昂贵后处理；
- 桌面 DPR 上限 1.5，移动和低画质 DPR 为 1，性能下降时逐级降档；
- 桌面目标 60 fps，移动目标 30 fps；后台标签页不持续绘制。

## 验证边界

单元测试覆盖数据完整性、18 条 HTTPS 链接、URL 白名单、偏好容错、能力检测、几何 attributes/Z 厚度、映射、负空间与轮廓 IoU；组件测试覆盖搜索、键盘、详情、最近访问、外链确认与降级；Playwright 覆盖桌面、平板、390/320、无 WebGL、reduced motion、历史恢复、横向溢出、WCAG 与几何视觉 catalog。构建后记录主包与三维分包体积。
